# 档口 iPad 选款系统 — 客户行为数据采集方案

> 构思时间：2026-06-03
> 状态：思路存档，暂不实现

---

## 一、背景

档口（嘉嘉服饰，广州御龙服装城）做非洲服装批发，客户以批发商为主。目前订货流程为手写碳纸单 → 拍照 OCR 录入系统。

未来计划用二手 iPad Pro 2018 放在档口，客户自行在上面选款、查库存、生成选购清单。

核心价值不止于替代手写单，还在于**客户行为数据的积累**。

---

## 二、数据采集的价值

档口是 B2B 批发场景，区别于零售：

- **客户复购率高** → 历史数据累积，越用越准
- **客户目标明确** → 行为数据干净，噪音低
- **面对面交易** → 数据 + 人工判断可互相验证

### 能学到什么

| 数据维度 | 应用场景 |
|---|---|
| 分类偏好 | 客户A偏爱连衣裙，客户B偏爱套装 |
| 颜色偏好 | 尼日利亚客户爱碎花/亮色，加纳客户爱纯色 |
| 价格带偏好 | 客户常看的是哪个价位段 |
| 停留时长 | 某款停留特别长但没下单 → 价格/库存问题 |
| 季节性规律 | 不同月份客户选款变化 → 提前备货 |

---

## 三、采集哪些数据

### 3.1 事件埋点

每个用户操作向后端发一条记录，结构如下：

```json
{
  "customer_id": "xxx",
  "event": "view_product",        // 事件类型
  "product_id": "JF-2026-001",    // 款号
  "color": "米白色",              // 选中的颜色
  "duration": 5,                  // 停留秒数
  "action": "add_to_cart",        // 操作：view / zoom / add / remove / confirm
  "timestamp": "2026-06-03T14:30:00Z"
}
```

### 3.2 事件类型

- `enter_category` — 进入某个分类
- `view_product` — 浏览款（含停留时长）
- `view_color` — 查看具体颜色
- `zoom_image` — 放大查看细节图
- `add_to_cart` — 加入选购清单
- `remove_from_cart` — 移除
- `confirm_order` — 确认下单

---

## 四、系统架构建议

```
iPad (Safari) ──→ 前端页面（档口模式）
                        │
                        ▼  API 埋点
              库存管理系统数据库
                        │
                        ▼
              客户行为表（单独）
                        │
                        ▼
              AI 分析 → 推荐 / 预警 / 报告
```

### 表结构（建议）

- `customer_behaviors`：原始行为流水，append-only
- `customer_profiles`：客户偏好画像（定时聚合更新）
- `product_trends`：款式的热度趋势

---

## 五、长期想象

1. **客户进店 iPad 自动识别**（Face ID / 扫码）→ 首页展示上次看的款
2. **智能推荐** → "您上次拿了碎花裙，这批到了新花色"
3. **进货辅助** → "过去两周有 5 个客户搜了这款，建议补货"
4. **定价参考** → 某款热门但询价多 → 可以考虑提价

---

## 六、落地节奏（未来参考）

| 阶段 | 内容 | 前置条件 |
|---|---|---|
| 第一阶段 | iPad 档口展示 + 选款 + 生成选购清单 | 库存系统前端做好 |
| 第二阶段 | 埋点 + 行为数据入库 | 后端加 API |
| 第三阶段 | 客户画像 + 报表 | 数据量够了开始 |
| 第四阶段 | AI 推荐 + 进货建议 | 数据模型训练 |

---

> 核心原则：**先跑起来，数据自然会有。**
> iPad 先当展示工具用，埋点一步一步加，不一口气做完。
