<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blade.system.permission.mapper.RolePermissionMapper">

    <delete id="deleteByRoleId">
        UPDATE sys_role_permission
        SET deleted = 1
        WHERE role_id = #{roleId}
        AND deleted = 0
    </delete>

    <delete id="deleteByPermissionId">
        UPDATE sys_role_permission
        SET deleted = 1
        WHERE permission_id = #{permissionId}
        AND deleted = 0
    </delete>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_role_permission (role_id, permission_id, tenant_id, deleted, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.permissionId}, #{item.tenantId}, 0, NOW())
        </foreach>
        ON DUPLICATE KEY UPDATE deleted = 0, create_time = NOW()
    </insert>

</mapper>
