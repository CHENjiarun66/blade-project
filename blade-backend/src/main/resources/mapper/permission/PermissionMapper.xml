<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blade.system.permission.mapper.PermissionMapper">

    <select id="selectCodesByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT p.code
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND p.deleted = 0
        AND rp.deleted = 0
    </select>

    <select id="selectCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT p.code
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND p.deleted = 0
        AND rp.deleted = 0
        AND ur.deleted = 0
    </select>

    <select id="selectByRoleId" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        AND p.deleted = 0
        AND rp.deleted = 0
    </select>

    <select id="selectByRoleIds" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND p.deleted = 0
        AND rp.deleted = 0
    </select>

    <select id="selectByType" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT *
        FROM sys_permission
        WHERE type = #{type}
        AND deleted = 0
        ORDER BY sort
    </select>

    <select id="selectMenusByUserId" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND p.type = 1
        AND p.deleted = 0
        AND rp.deleted = 0
        AND ur.deleted = 0
        ORDER BY p.sort
    </select>

    <select id="selectButtonsByUserId" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND p.type = 2
        AND p.deleted = 0
        AND rp.deleted = 0
        AND ur.deleted = 0
        ORDER BY p.sort
    </select>

    <select id="selectFieldsByUserId" resultType="com.blade.system.permission.entity.SysPermission">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND p.type = 3
        AND p.deleted = 0
        AND rp.deleted = 0
        AND ur.deleted = 0
    </select>

</mapper>
